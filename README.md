# Callbacks, promesas, await

Ejemplos de utilización de callbacks, promesas y await.

### Notas:
Recuerda construir los módulos de Node

```
npm install
```
y construir el build para crear el directorio /dist:
```
npm run build
```